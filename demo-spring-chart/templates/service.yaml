apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-spring-chart.fullname" . }}
  labels:
    {{- include "demo-spring-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-spring-chart.selectorLabels" . | nindent 4 }}
