apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-spring-chart.fullname" . }}-test-connection"
  labels:
    {{- include "demo-spring-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-spring-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
